# ASI02: Sensitive Data Disclosure — 输出关
id: asi02-sensitive-data
owasp_id: ASI02
title: Sensitive Data Disclosure
severity: high
category: output
description: |
  The agent may expose sensitive data (PII, API keys, credentials, internal
  system prompts) through its outputs without proper filtering or redaction.
patterns:
  - "(api[_-]?key|apikey|secret[_-]?key)\\s*[:=]\\s*['\"]?[a-zA-Z0-9]{16,}"
  - "(password|passwd|pwd)\\s*[:=]\\s*['\"][^'\"]{6,}"
  - "(bearer|token)\\s+(eyJ[a-zA-Z0-9]{10,}|[a-f0-9]{32,})"
  - "(aws_access_key_id|aws_secret_access_key)\\s*[:=]"
  - "-----BEGIN (RSA |EC |DSA )?PRIVATE KEY-----"
  - "(database_url|db_url|connection_string)\\s*[:=]\\s*['\"]?(mysql|postgres|mongodb)://[^\\s]{10,}"
fix: |
  1. Add output filtering/redaction for PII and credentials
  2. Never include raw credentials in prompts or tool outputs
  3. Implement data classification and access controls
  4. Reference: OWASP ASI02 mitigation guide
